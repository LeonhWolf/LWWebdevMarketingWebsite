import type Ii18nStrings from "./index.types";
import type { INavbar } from "./index.types";

const navbar: INavbar = {
  service: "service",
  process: "prozess",
  references: "referenzen",
  about: "über mich",
};

const i18nStrings: Ii18nStrings = {
  navbar,
};
export default i18nStrings;
